# Kafka Docker Compose Start Script

Below is a bash script that can be used to start the Kafka docker-compose file:
    
```bash
# Start the services
docker-compose up -d
```

```bash
# Stop the services
docker-compose down
```
